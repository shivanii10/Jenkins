services:
  database:
    image: redis
  web:
    image: nginx
    ports: 
     - 9090:80

docker-compose config
docker-compose up -d
docker-compose down
docker-compose up -d --scale database=4

